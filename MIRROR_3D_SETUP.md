# 2층 친구의 방 거울 — 3D 시차(parallax) 적용 가이드

> 기존: 미러룸 + **고정 카메라** → 시점 움직여도 거울 속이 안 변해 "2D 같음".
> 변경: 미러룸 카메라에 플레이어 움직임만큼 시차를 더해 깊이감(3D 거울) 부여.
> 반사 행렬/oblique/스케일 문제 없음. **이미 잘 맞던 카메라 배치를 그대로 재사용.**

## 스크립트

`Assets/ObjectScripts/MirrorRoomCameraTracker.cs`

**원리:** 게임 시작 시 ①미러 카메라의 현재 위치(이미 잘 맞던 그 배치)와 ②플레이어 기준 시점을
캡처 → 이후 플레이어가 기준점에서 움직인 만큼만 카메라를 이동/회전.
→ 기본 화면은 항상 맞고, 움직이면 시차가 생김.

## 셋업 (roomAnchor·realMirror 필요 없음!)

1. **미러룸 카메라**(RT_Mirror에 출력 중, 위치가 이미 잘 맞는 그 카메라)에
   `MirrorRoomCameraTracker` 부착

2. 슬롯 연결:

| 필드 | 연결 |
|---|---|
| mirrorCamera | (비우면 자기 자신 Camera) |
| playerCamera | 플레이어 메인 카메라 Transform |
| playerReferencePoint | 빈 GameObject를 **"플레이어가 거울 앞에서 보는 위치(눈높이)"** 에 두고 연결 |
| matchPlayerFOV | 보통 끔 (미러 카메라 FOV 튜닝값 유지) |
| keepUpright | ✅ (귀신 눕는 현상 방지) |
| parallaxStrength | 1 (작게 하면 시차 은은해짐) |
| activationDistance | 6 (기준점에서 6m 밖이면 카메라 꺼 성능 절약. 0=항상 켬) |

3. **playerReferencePoint 만들기** (이것만 하면 됨)
   - 친구의 방 거울 앞, **플레이어가 서서 거울을 볼 위치**에 빈 오브젝트 생성
   - 높이는 플레이어 눈높이쯤
   - 회전은 **거울을 바라보는 방향**으로 (대충 거울 정면)
   - → 미러 카메라의 "기본 화면"이 이 시점 기준으로 잠김

> **중요:** 미러 카메라는 지금 잘 맞는 위치 그대로 두세요. 스크립트가 그 위치를
> 기준으로 삼습니다. 카메라가 원래 안 맞았으면 먼저 카메라부터 제대로 배치할 것.

## 동작 확인

1. activationDistance를 임시로 0 (항상 켬)
2. Play → 거울 앞에서 좌우/앞뒤로 움직임
3. 거울 속이 **시차를 두고 따라 움직이면 성공**
4. 기본 위치(기준점 근처)에선 예전처럼 잘 맞아야 함

## 미세 조정 / 문제 해결

| 증상 | 조치 |
|---|---|
| 기본 화면이 안 맞음 | 미러 카메라 자체 배치가 틀린 것 → 카메라를 거울에 맞게 먼저 배치 후 재생 |
| 시차가 너무 과함/이상함 | parallaxStrength를 0.3~0.6으로 낮추기 |
| 귀신이 90° 누움 | keepUpright 켜기 |
| 좌우가 진짜 거울처럼 안 뒤집힘 | 거울 머티리얼 Base Map **Tiling X = -1** |
| 거울 속이 검정 | 미러룸에 빛 있나 / 거울 머티리얼 Base Color 흰색(#FFFFFF)인가 / 미러 카메라 Target Texture=RT_Mirror 인가 |
| 끊김 의심 | activationDistance 줄여 멀 때 렌더 멈추기 |
| 시작 시 플레이어가 거울에서 멀다 | playerReferencePoint를 반드시 지정(자동 캡처 대신) |
